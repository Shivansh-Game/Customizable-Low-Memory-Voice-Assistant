# Customizable-Low-Memory-Voice-Assistant
A voice assistant designed to use as little memory as possible while being useful. It can call external python scripts, run functions that you define and has TTS functionality too. Memory footprint: ~300 MB.
